2. Unit tests — tests/unit/recsys/
Pure functions worth testing in isolation:
test_curation_logic.py — all three methods are pure:

_extract_book_ids_from_citations: duplicate citations (should deduplicate), malformed links ignored, non-integer IDs ignored, empty text, multiple citations
_order_books_by_citations: citation order respected, IDs not in candidates silently dropped, empty citation list
_prepare_candidates: vibe truncated at 200 chars, to_curation_dict() used when available, fallback dict built correctly when not

test_planner_parsing.py:

_parse_strategy_response: strips ```json fences, parses valid JSON, attaches profile_data, raises KeyError on missing required fields, raises json.JSONDecodeError on invalid JSON

test_orchestrator_init.py — warm/cold threshold logic:

user_num_ratings=9, warm_threshold=10 → _has_als_recs=False, als_recs not in available tools
user_num_ratings=10, warm_threshold=10 → _has_als_recs=True, als_recs first in tool list
user_num_ratings=None → defaults to 0, cold user
The _build_curation_fallback_prose() helper (new method you'll add for the fallback) — pure function, deterministic output from a known candidate list

3. Integration tests — tests/integration/chatbot/agents/recommendation/
conftest.py — rebuild all builders using AsyncMock:

mock_planner_builder — AsyncMock returning PlannerStrategy
mock_retrieval_builder — AsyncMock returning RetrievalOutput
mock_selection_builder — AsyncMock returning List[BookRecommendation] (different return type from the others)
mock_curation_builder — AsyncMock whose execute_stream() is an async generator yielding StreamChunks (not an async function — this is the tricky one since you need AsyncMock that returns an async generator)
Fix tool name: "als_recs" not "als_recommendations" throughout strategy_factory

test_pipeline_integration.py — all tests rewritten around execute_stream():
Tests collect chunks via:
pythonchunks = []
async for chunk in agent.execute_stream(request):
    chunks.append(chunk)
complete = next(c for c in chunks if c.type == "complete")
Classes:

TestStageTransitions — what reaches each stage (Planner → Retrieval → Selection → Curation)
TestSelectionStageTransitions — new class: Selection receives retrieval's full pool; Curation receives Selection's subset
TestParameterPropagation — same as before but async
TestErrorHandling — verify each stage's fallback: planning/retrieval failures yield success=False complete chunk; selection failure still reaches curation with top 10; curation failure yields the markdown prose with valid book_ids extracted
TestFullPipelineFlow — end-to-end chunk sequence checks (status chunks appear in right order, token chunks before complete, complete has book_ids)

4. run_all_recommendations_tests.py — add Selection stage
Add Selection as Stage 3 between Retrieval and Curation, merge its results into the unified output. Small change.

One open question
For mocking CurationAgent.execute_stream() (which is an async generator), AsyncMock doesn't directly support this — you need to assign an AsyncMock whose return value is an async iterator. I'll handle this with a helper fixture that creates an AsyncMock wrapping a real async generator function. Does that approach work for you, or do you have a preference for how mock async generators are structured in your test setup?
