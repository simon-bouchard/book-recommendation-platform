# requirements/ci.txt
#
# Minimal dependencies for running unit tests and linting in CI.
# Does NOT include heavy inference deps (GPU torch, FAISS-GPU, etc.)
#
# Keep in sync with what tests/unit/ actually imports.

# --- linting / formatting ---
ruff==0.14.6

# --- test runner ---
pytest==9.0.0
pytest-asyncio==1.3.0
respx==0.22.0

# --- web framework ---
fastapi==0.116.1
starlette==0.47.2
httpx==0.28.1
pydantic==2.11.7
pydantic-core==2.33.2
pydantic-settings==2.10.1
anyio==4.10.0

# --- database / cache ---
sqlalchemy==2.0.43
aiomysql==0.3.2
redis==6.4.0

# --- auth ---
bcrypt==4.3.0
itsdangerous==2.2.0
PyJWT==2.10.1
python-dotenv==1.1.1

# --- data / ML ---
numpy==1.26.4
pandas==2.3.2
scipy
lightgbm==4.6.0
faiss-cpu==1.7.4
implicit==0.7.2
tqdm==4.67.1
orjson==3.11.3

# --- NLP / embeddings ---
sentence-transformers==5.1.0
langcodes==3.5.0
pycountry==24.6.1

# --- search ---
meilisearch==0.38.0

# --- LLM / agent ---
langchain-core==0.3.76
langchain==0.3.27
langchain-community==0.3.29
langchain-openai==0.3.33
langgraph==0.6.7
ddgs==9.5.5
wikipedia==1.4.0

# --- metrics ---
prometheus-client==0.24.1
prometheus-fastapi-instrumentator==7.1.0

# --- observability ---
opentelemetry-api==1.40.0
opentelemetry-sdk==1.40.0
opentelemetry-exporter-otlp-proto-grpc==1.40.0
opentelemetry-exporter-otlp-proto-http==1.40.0
opentelemetry-instrumentation-fastapi==0.61b0
opentelemetry-instrumentation-sqlalchemy==0.61b0
opentelemetry-instrumentation-httpx==0.61b0

# torch is installed separately in CI via --index-url to get the CPU-only wheel
# See .github/workflows/ci.yml
