# requirements/semantic.txt
#
# Semantic search model server dependencies.
# faiss-cpu: SemanticSearcher loads and queries a FAISS index at runtime.
# sentence-transformers: encodes free-text queries into float32 vectors.

-r base.txt
faiss-cpu==1.9.0
sentence-transformers==5.1.0
